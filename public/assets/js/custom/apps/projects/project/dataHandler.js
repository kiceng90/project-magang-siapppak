// const monthNodes = document.querySelectorAll('.month_selected');
// // handle click

// const getData = ()

// const handleClick = (e) => console.log(e.target.dataset.month);
// Array.from(monthNodes).map(node => {
//     node.addEventListener('click', handleClick)
// });
